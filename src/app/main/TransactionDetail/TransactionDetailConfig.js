
import TransactionDetail from './TransactionDetail';

const TransactionDetailConfig = {
  settings: {
    layout: {
      config: {},
    },
  },
  routes: [
    {
      path: '/txn_detail',
      component: TransactionDetail,
    },
  ],
};

export default TransactionDetailConfig;
