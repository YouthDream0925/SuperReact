
import Blocks from './Blocks';

const BlocksConfig = {
  settings: {
    layout: {
      config: {},
    },
  },
  routes: [
    {
      path: '/blocks',
      component: Blocks,
    },
  ],
};

export default BlocksConfig;
