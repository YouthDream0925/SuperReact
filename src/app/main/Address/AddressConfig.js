import Address from './Address';

const AddressConfig = {
  settings: {
    layout: {
      config: {},
    },
  },
  routes: [
    {
      path: '/address',
      component: Address,
    },
  ],
};

export default AddressConfig;
